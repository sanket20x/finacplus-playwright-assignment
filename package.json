{
  "name": "finacplus-playwright",
  "version": "1.0.0",
  "description": "QA Automation Assignment using Playwright",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "dependencies": {
    "@playwright/test": "^1.58.1",
    "dotenv": "^16.6.1"
  }
}
